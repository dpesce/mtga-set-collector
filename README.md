# MTGA Set Collection Strategy (GitHub Pages)

Static site hosted on GitHub Pages.

## Local preview
Because the calculator fetches JSON, serve locally (don’t open the HTML file directly):

```bash
cd docs
python -m http.server 8000
